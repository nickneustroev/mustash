services:
  auto-playlists:
    build:
      context: .
      dockerfile: apps/auto-playlists/Dockerfile
    container_name: spotify-helper-auto-playlists
    env_file:
      - .env
    ports:
      - "${SPOTIFY_LISTEN_PORT:-3000}:${SPOTIFY_LISTEN_PORT:-3000}"
    restart: unless-stopped
