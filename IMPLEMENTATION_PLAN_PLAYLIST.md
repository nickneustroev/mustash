# План реализации: Auto Playlist `HISTORY [AUTO]`

## 1. Расширение конфигурации и scopes

- [x] Добавить новые env-переменные:
- [x] `HISTORY_PLAYLIST_NAME` (дефолт: `HISTORY [AUTO]`).
- [x] `HISTORY_MAX_ITEMS` (дефолт: `100`, ограничить `<=100`).
- [x] `HISTORY_STATE_PATH` (дефолт: `.history-state.json`).
- [x] `PLAYLIST_SYNC_DEBOUNCE_MS` (дефолт: `7000`).
- [x] `BACKFILL_INTERVAL_MS` (дефолт: `60000`).
- [x] `BACKFILL_LIMIT` (дефолт: `50`).
- [x] Добавить новые scopes в OAuth:
- [x] `user-read-recently-played`
- [x] `playlist-modify-private`
- [x] `playlist-read-private`
- [x] Обновить валидацию конфига.

Критерий готовности:
- Приложение стартует с новыми env и корректно запрашивает расширенные права.

## 2. Модель истории и локальное хранилище

- [x] Создать типы для записи истории (`trackUri`, `playedAt`, `source`).
- [x] Реализовать `HistoryStore` с rolling-окном `N=100`.
- [x] Разрешить дубликаты треков без дедупликации.
- [x] Реализовать загрузку состояния из файла.
- [x] Реализовать сохранение состояния в файл.
- [x] Добавить защиту от поврежденного state-файла.

Критерий готовности:
- История корректно переживает перезапуск и всегда ограничена 100 элементами.

## 3. PlaylistManager

- [x] Реализовать получение `userId` через `GET /v1/me`.
- [x] Реализовать поиск плейлиста по имени `HISTORY [AUTO]`.
- [x] Реализовать создание плейлиста при отсутствии.
- [x] Реализовать `replaceItems(trackUris)` через `PUT /playlists/{id}/tracks`.
- [x] Добавить retry после `401` (через существующий refresh flow).
- [x] Добавить обработку `429` и backoff.

Критерий готовности:
- Плейлист `HISTORY [AUTO]` автоматически создается и полностью заменяется по локальной истории.

## 4. Интеграция live-событий в историю

- [x] На `NewTrackEvent` извлекать `trackUri`.
- [x] Пропускать невалидные элементы (без URI, не `track`).
- [x] Добавлять запись в `HistoryStore` (newest -> oldest).
- [x] Помечать состояние как `dirty` для отложенной sync.

Критерий готовности:
- Каждый новый трек из live-потока попадает в локальную историю.

## 5. Debounce-синхронизация плейлиста

- [x] Реализовать `PlaylistSyncScheduler`.
- [x] При `dirty` запускать отложенный sync (`PLAYLIST_SYNC_DEBOUNCE_MS`).
- [x] На sync отправлять полный массив `items` в `replaceItems`.
- [x] Не запускать параллельные sync.
- [x] Гарантировать финальный sync при shutdown (`SIGINT`/`SIGTERM`).

Критерий готовности:
- Плейлист обновляется пакетно, без API-спама, и не теряет последние изменения при остановке.

## 6. Backfill из recently played

- [x] Реализовать `BackfillService`.
- [x] Запуск backfill на старте приложения.
- [x] Периодический backfill по `BACKFILL_INTERVAL_MS`.
- [x] Запрашивать `GET /v1/me/player/recently-played?limit=BACKFILL_LIMIT`.
- [x] Корректно маппить `played_at` и `track.uri`.
- [x] Добавлять в историю в правильном временном порядке.
- [x] При изменении истории помечать `dirty`.

Критерий готовности:
- Пропуски после простоя добираются автоматически.

## 7. Устойчивость и отказоустойчивость

- [x] Обработать `429` для playlist/backfill потоков через backoff.
- [x] Согласовать поведение при временных сетевых ошибках (warn + retry позже).
- [x] При ошибке записи файла состояния не падать полностью.
- [x] Добавить защиту от гонок между live-добавлением и backfill.

Критерий готовности:
- Приложение продолжает работать при временных ошибках и восстанавливается самостоятельно.

## 8. Unit-тесты

- [x] `HistoryStore`: окно 100, порядок, разрешенные дубликаты.
- [x] `PlaylistSyncScheduler`: debounce, no-parallel-sync, flush-on-shutdown.
- [x] `BackfillService`: корректный порядок вставки и пропуски невалидных элементов.
- [x] `PlaylistManager`: create-or-find логика и replace payload.
- [x] Ошибочные ветки: `401`, `429`.

Критерий готовности:
- Критичные сценарии playlist-функциональности покрыты unit-тестами.

## 9. Ручная приемка

- [ ] Очистить/создать тестовый плейлист `HISTORY [AUTO]`.
- [ ] Послушать 5-10 треков с повторами.
- [ ] Проверить, что повторы сохраняются в плейлисте.
- [ ] Проверить, что новые треки появляются сверху.
- [ ] Проверить limit=100 (старые вытесняются).
- [ ] Остановить приложение, снова запустить, проверить continuity через backfill.
- [ ] Проверить поведение при ручном изменении плейлиста (следующая sync восстанавливает историю).

Критерий готовности:
- Реальный плейлист в Spotify отражает последние 100 прослушиваний с повторами.

## 10. Документация

- [x] Обновить `README.md` (новые scopes и env).
- [x] Обновить `.env.example` (playlist-настройки).
- [x] Добавить раздел "Как работает HISTORY [AUTO]".
- [x] Добавить раздел "Ограничения и ожидаемое поведение".

Критерий готовности:
- Новый пользователь может включить функциональность playlist-history только по документации.
