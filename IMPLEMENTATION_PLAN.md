# План реализации (чеклист)

## 1. Подготовка проекта

- [x] Инициализировать Node.js + TypeScript CLI-проект.
- [x] Подключить линтер/форматтер.
- [x] Добавить npm-скрипты: `dev`, `build`, `start`, `test`.
- [x] Настроить загрузку `.env`.
- [x] Добавить валидацию runtime-конфига.
- [x] Проверить, что конфиг логируется без секретов.

Критерий готовности:
- Приложение стартует и валидирует конфигурацию.

## 2. OAuth и токены

- [x] Реализовать `AuthManager` (Authorization Code flow).
- [x] Поднять локальный callback endpoint по `SPOTIFY_REDIRECT_URI`.
- [x] Реализовать хранение токенов в `TOKEN_STORAGE_PATH`.
- [x] Реализовать загрузку токенов при старте.
- [x] Добавить refresh access token по истечению.
- [x] Добавить retry одного API-запроса после refresh.

Критерий готовности:
- Первый запуск проходит через логин, повторный запуск работает без нового логина.

## 3. Spotify API клиент

- [x] Реализовать `SpotifyClient`.
- [x] Добавить `getCurrentlyPlaying()`.
- [x] Нормализовать ответ в `PlaybackSnapshot`.
- [x] Обработать `204 No Content`.
- [x] Обработать `401 Unauthorized`.
- [x] Обработать `429 Too Many Requests` + `Retry-After`.
- [x] Обработать временные `5xx` и сетевые ошибки.

Критерий готовности:
- Клиент стабильно возвращает `PlaybackSnapshot` или `null`.

## 4. Логика отслеживания трека

- [x] Реализовать `TrackWatcher` с polling по `POLL_INTERVAL_MS`.
- [x] Хранить `lastReportedTrackId`.
- [x] Эмитить событие только при смене `trackId`.
- [x] Не эмитить при `isPlaying=false`.
- [x] Игнорировать `episode`, `ad`, `unknown`.
- [x] Реализовать `PRINT_ON_START=true`.
- [x] Реализовать `PRINT_ON_START=false`.

Критерий готовности:
- Один новый трек = одна строка вывода, без дублей.

## 5. Вывод и lifecycle

- [x] Реализовать `ConsoleNotifier`.
- [x] Формат вывода: `Artist1, Artist2 - Track Name`.
- [x] Собрать все компоненты в `App`.
- [x] Добавить обработку `SIGINT`.
- [x] Добавить обработку `SIGTERM`.
- [x] Реализовать корректную остановку таймера и завершение процесса.

Критерий готовности:
- Приложение стабильно стартует и завершается без ошибок.

## 6. Тесты

- [x] Unit: детекция смены трека.
- [x] Unit: стартовое поведение (`PRINT_ON_START=true/false`).
- [x] Unit: нормализация payload Spotify.
- [x] Unit: refresh/retry после `401`.
- [x] Unit: backoff при `429`.

Критерий готовности:
- Все критичные unit-тесты проходят.

## 7. Ручная приемка

- [ ] Проверить запуск при активном воспроизведении.
- [ ] Проверить смену трека.
- [ ] Проверить паузу.
- [ ] Проверить возобновление того же трека (без дубля).
- [ ] Проверить skip на следующий трек.
- [ ] Проверить истечение access token.
- [ ] Проверить сценарий rate-limit/backoff.

Критерий готовности:
- Все приемочные сценарии проходят локально.

## 8. Документация и финализация

- [x] Обновить `README.md` фактическими командами.
- [x] Добавить `.env.example`.
- [x] Добавить раздел с ограничениями MVP.
- [x] Проверить, что секреты не попадают в git.

Критерий готовности:
- Проект можно запустить по документации без дополнительных пояснений.
